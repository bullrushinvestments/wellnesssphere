interface BuildCommand {
  command: string;
  output?: string;
}

interface EnvTemplate {
  [key: string]: string;
}

interface HeaderRule {
  source: string;
  headers: Array<{ key: string; value: string }>;
}

interface CacheStrategy {
  [path: string]: { regexPattern: string; edgeTtl?: number };
}

interface FrameworkSettings {
  vercel?: Record<string, any>;
  netlify?: Record<string, any>;
}

interface DeployConfig {
  build: BuildCommand;
  env: EnvTemplate;
  headers: Array<HeaderRule>;
  cache: CacheStrategy;
  framework: FrameworkSettings;
}

export const deployConfig: DeployConfig = {
  build: {
    command: "next build",
    output: ".next"
  },
  env: {
    NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL,
    NEXT_PUBLIC_STRIPE_KEY: process.env.NEXT_PUBLIC_STRIPE_KEY
  },
  headers: [
    {
      source: "/(.*)",
      headers: [
        { key: "Strict-Transport-Security", value: "max-age=31536000; includeSubDomains; preload" },
        { key: "X-Frame-Options", value: "DENY" },
        { key: "X-XSS-Protection", value: "1; mode=block" },
        { key: "X-Content-Type-Options", value: "nosniff" }
      ]
    }
  ],
  cache: {
    "/api/(.*)": { regexPattern: "^/api/", edgeTtl: 0 },
    "/(static)/(.*)": { regexPattern: "^/(static)/" }
  },
  framework: {
    vercel: {
      buildCommand: "next build",
      startCommand: "next start"
    },
    netlify: {
      publish: ".next/*"
    }
  }
};

interface BuildCommand {
  command: string;
  output?: string;
}

interface EnvTemplate {
  [key: string]: string;
}

interface HeaderRule {
  source: string;
  headers: Array<{ key: string; value: string }>;
}

interface CacheStrategy {
  [path: string]: { regexPattern: string; edgeTtl?: number };
}

interface FrameworkSettings {
  vercel?: Record<string, any>;
  netlify?: Record<string, any>;
}

interface DeployConfig {
  build: BuildCommand;
  env: EnvTemplate;
  headers: Array<HeaderRule>;
  cache: CacheStrategy;
  framework: FrameworkSettings;
}

export const deployConfig: DeployConfig = {
  build: {
    command: "next build",
    output: ".next"
  },
  env: {
    NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL,
    NEXT_PUBLIC_STRIPE_KEY: process.env.NEXT_PUBLIC_STRIPE_KEY
  },
  headers: [
    {
      source: "/(.*)",
      headers: [
        { key: "Strict-Transport-Security", value: "max-age=31536000; includeSubDomains; preload" },
        { key: "X-Frame-Options", value: "DENY" },
        { key: "X-XSS-Protection", value: "1; mode=block" },
        { key: "X-Content-Type-Options", value: "nosniff" }
      ]
    }
  ],
  cache: {
    "/api/(.*)": { regexPattern: "^/api/", edgeTtl: 0 },
    "/(static)/(.*)": { regexPattern: "^/(static)/" }
  },
  framework: {
    vercel: {
      buildCommand: "next build",
      startCommand: "next start"
    },
    netlify: {
      publish: ".next/*"
    }
  }
};